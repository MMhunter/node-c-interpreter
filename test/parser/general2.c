int i = 0;
int j = 0;

void loop() {

    sysmaster.serial.println(i+"\" +j);/*串口打印（内容：数字／变量／"字符串"）；显示完内容后自动换行*/

void loop() {
    sysmaster.serial.println(i+"\" +j);/*串口打印（内容：数字／变量／"字符串"）；显示完内容后自动换行*/"

int
